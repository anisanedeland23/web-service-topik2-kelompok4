services:
  mongodb:
    image: mongo:7.0
    container_name: mongodb-local
    restart: unless-stopped
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_DATABASE: inventorydb
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
